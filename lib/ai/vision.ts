export async function identifyFoodImage(dataUrl: string) {
  // Call your AI service (e.g., OpenAI or another model)
  // For now, we mock the response

  return {
    dishName: "Pizza",
    confidence: 95, // Confidence score
  }
}
